class Solution {
    public int reverseBits(int n) {
        int[] bits=new int[32];
        for(int i=0;i<32;i++)
            bits[i]=(n>>i) & 1;
        int ans=0;
        for(int i=0;i<32;i++) {
            ans+=bits[i]*(1<<(31-i));
        }
        return ans;
    }
}