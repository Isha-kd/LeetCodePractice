class Solution {
    public boolean checkDivisibility(int n) {
        int sum = 0, prod = 1, m = n;
        while(m > 0) {
            int a = m % 10;
            sum += a;
            prod *= a;
            m /= 10;
        }
        return n % (sum + prod) == 0;
    }
}