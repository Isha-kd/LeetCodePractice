class Solution {
    public int largestAltitude(int[] gain) {
        int s = 0, max = 0;
        for(int i : gain) {
            s += i;
            max = Math.max(s, max);
        }
        return max;
    }
}